'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertArrayOfArraysToCSV = undefined;

var _entries = require('babel-runtime/core-js/object/entries');

var _entries2 = _interopRequireDefault(_entries);

var _toConsumableArray2 = require('babel-runtime/helpers/toConsumableArray');

var _toConsumableArray3 = _interopRequireDefault(_toConsumableArray2);

var _checkSpecialCharsAndEmpty = require('../helpers/check-special-chars-and-empty');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var convertArrayOfArraysToCSV = exports.convertArrayOfArraysToCSV = function convertArrayOfArraysToCSV(data, _ref) {
  var header = _ref.header,
      separator = _ref.separator;

  var array = [].concat((0, _toConsumableArray3.default)(data));
  var csv = '';

  if (header) {
    header.forEach(function (headerEl, i) {
      var includesSpecials = (0, _checkSpecialCharsAndEmpty.checkSpecialCharsAndEmpty)(headerEl);
      csv += (includesSpecials ? '"' + headerEl + '"' : headerEl) + ((0, _entries2.default)(header).length - 1 === i ? '' : separator) + ((0, _entries2.default)(header).length - 1 === i ? '\n' : '');
    });
  }

  array.forEach(function (row) {
    row.forEach(function (value, i) {
      var includesSpecials = (0, _checkSpecialCharsAndEmpty.checkSpecialCharsAndEmpty)(value);
      csv += (includesSpecials ? '"' + value + '"' : value) + (row.length - 1 === i ? '' : separator) + (row.length - 1 === i ? '\n' : '');
    });
  });

  return csv;
};